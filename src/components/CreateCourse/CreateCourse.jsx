import React from "react";
import Button from "../Button/Button";

const CreateCourse = () => {
  return (
    <div>
      <Button size="medium" text="Add new post" />
    </div>
  );
};

export default CreateCourse;
